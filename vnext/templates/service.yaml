apiVersion: v1
kind: Service
metadata:
  name: {{ include "vnext.fullname" . }}
  labels:
    {{- include "vnext.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vnext.selectorLabels" . | nindent 4 }}
